<h1>Galpões e Estoque</h1>

<% if @warehouses.empty?%>
    <p>Não existem galpões cadastrados</p>
<% end %>
<div>
  <% @warehouses.each do |w| %>
  <dl>
   <strong> Nome: <%= link_to(w.name, warehouse_path(w.id))%></a></strong>
   <strong> Código: <%= w.code%></strong>
   <strong> Cidade: <%= w.city%></strong>
   <strong>Área: <%= w.area %> m²</strong>
  <% end %>
  </dl>
</div>

<%= link_to('Cadastrar Galpão', new_warehouse_path)%>