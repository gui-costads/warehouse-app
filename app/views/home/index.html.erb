<h1>Galpões e Estoque</h1>

<h2>
  <% @warehouses.each do |w| %>
    Nome: <%= link_to(w.name, warehouse_path(w.id))%></a>
    Código: <%= w.code%>
    Cidade: <%= w.city%>
    Área: <%= w.area %> m²
  <% end %>
</h2>

<% if @warehouses.empty?%>
    <p>Não existem galpões cadastrados</p>
<% end %>